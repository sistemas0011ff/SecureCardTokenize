export interface IRequestPrograma {
    programa: {
        canal: string;
        sucursal_rq: string;
        nro_caja_rq: string;
        nro_transaccion_rq: string;
        fecha_hora_rq: string;
        vendedor_rq: string;
        version: string;
        timeout: string;
    }
}